export type requestCreateCollection = {
    latitude: number,
    longitude: number,
    center: number,
    name: string,
    description: string,
    score_is_calculated: boolean,
    final_score_is_calculated: boolean,
}